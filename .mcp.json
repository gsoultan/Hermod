{
  "mcpServers": {
    "postgres": {
      "command": "uvx",
      "args": [
        "--with",
        "mcp<2",
        "postgres-mcp",
        "--access-mode=restricted",
        "postgresql://postgres:postgres@localhost:5432/hermod_metadata"
      ]
    }
  }
}
